---

- name: Remove existing cluster "{{ Orchestrator_cluster_name }}" from DCS
  ansible.builtin.expect:
    command: "Orchestratorctl -c {{ Orchestrator_config_file }} remove {{ Orchestrator_cluster_name }}"
    responses:
      (.*)Please confirm the cluster name to remove: "{{ Orchestrator_cluster_name }}"
      (.*)"Yes I am aware": "Yes I am aware"
  environment:
    PATH: "{{ ansible_env.PATH }}:/usr/bin:/usr/local/bin"
  vars:
    ansible_python_interpreter: /usr/bin/python3
  when:
    - inventory_hostname in groups['primary']

...
