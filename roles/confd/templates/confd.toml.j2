backend = "etcdv3"
interval = 10
watch = true
nodes = [
{% if not dcs_exists|bool and dcs_type == 'etcd' %}
  {% for host in groups['etcd_cluster'] %}
  "http://{{ hostvars[host]['inventory_hostname'] }}:2379",
  {% endfor %}
{% endif %}
{% if dcs_exists|bool and dcs_type == 'etcd' %}
  {% for etcd_hosts in Orchestrator_etcd_hosts %}
  "{{ Orchestrator_etcd_protocol | default('http', true) }}://{{etcd_hosts.host}}:{{etcd_hosts.port}}",
  {% endfor %}
{% endif %}
]
{% if dcs_exists|bool and dcs_type == 'etcd' %}
{% if Orchestrator_etcd_username | default('') | length > 0 %}
basic_auth = true
username = "{{ Orchestrator_etcd_username | default('') }}"
password = "{{ Orchestrator_etcd_password | default('') }}"
{% endif %}
{% endif %}