[{{ pgbackrest_stanza }}]
{% for host in groups['postgres_cluster'] %}
pg{{ loop.index }}-host={{ host }}
pg{{ loop.index }}-port={{ MySQL_port }}
pg{{ loop.index }}-path={{ MySQL_data_dir }}
{% endfor %}

